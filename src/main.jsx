import React from "react";
import ReactDOM from "react-dom/client";
import HomepageMockup from "./index";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <HomepageMockup />
  </React.StrictMode>
);
